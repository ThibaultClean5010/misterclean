# Server-only Vercel variables. Never use a VITE_ prefix for these values.
# Enable only after verifying the sender domain and testing inbox delivery.
QUOTE_FORM_ENABLED=false
RESEND_API_KEY=
QUOTE_FROM_EMAIL=
